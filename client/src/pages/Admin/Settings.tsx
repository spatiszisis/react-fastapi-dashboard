import AdminLayout from "../../components/AdminLayout";

const Settings = () => {
  return (
    <AdminLayout title="Settings" subtitle="Manage your settings">
      Settings page
    </AdminLayout>
  );
};

export default Settings;
